(import (lua io))
(import (lua basic))
(import (lua string))
(import (lua table))
(import (rt util))
(let ((f (lua-io-lines "pw.txt"))
      (line (f)))
  (display (str (lua-table-unpack (map (lambda (s) (lua-string-char (lua-tonumber s 16))) (util-re line ["," "s"]))))))
(newline)
